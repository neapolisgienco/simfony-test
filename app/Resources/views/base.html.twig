<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>555
    <!--9 урок 7 минута.30-->
    {% for product_id in productsMassiv %}
        <a href="">Product {{ product_id}}</a><br>
    {% endfor %}
    <br><br>
    {% for product in products %}
        <a href="">Product base  {{ product.id}}-{{ product.title}}</a><br>
    {% endfor %}

        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
